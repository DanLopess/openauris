import SwiftUI

// Legacy placeholder retained so existing previews still open quickly.
struct ContentView: View {
    var body: some View {
        Text("OpenAuris")
            .font(.title)
            .padding()
    }
}

#Preview {
    ContentView()
}
